# Flask Configuration
# Set to 'development' for development mode, 'production' for production
FLASK_ENV=production

# Debug mode should be 'true' only in development for performance reasons
# In production, set to 'false' to avoid performance degradation
FLASK_DEBUG=false

# Flask server configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
